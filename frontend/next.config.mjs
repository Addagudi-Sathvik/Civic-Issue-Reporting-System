/** @type {import('next').NextConfig} */
const nextConfig = {
  // Environment variables are automatically picked from .env.local and .env.production
  // NEXT_PUBLIC_* variables are available in the browser
};

export default nextConfig;

